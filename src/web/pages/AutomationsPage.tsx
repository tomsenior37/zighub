export function AutomationsPage() {
  return (
    <section className="mx-auto max-w-2xl px-6 py-10">
      <h1 className="text-2xl font-semibold text-slate-900">Automations</h1>
      <p className="mt-3 text-slate-600">
        Automations defined as YAML drive zighub&rsquo;s rule engine. Visual builder, manual YAML
        editing, and LLM-assisted authoring via MCP land in later iterations. Drafts require
        explicit user approval before going live.
      </p>
    </section>
  );
}
